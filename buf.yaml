version: v2
# Timestamp C++ Implementation
# Uses buf CLI to fetch APIs directly from GitHub
modules:
  - path: .
    name: buf.build/esonti/timestamp-impl
deps:
  # Fetch timestamp API directly from GitHub
  - github.com/esonti/protobuf-sm-timestamp-api
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
